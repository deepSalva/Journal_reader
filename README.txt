Hi! this is a Headline reader which analyze the most common words in 4 different Newspaper.

If you want to run it you just need to save the ".py" file and run it with your Shell (Git, Ubuntu, Powershell...)

You can change the Newspaper websites just changing the "url" in main() Arguments. And also the names of the newspaper
inside start() function, "intro" variable.